random类中生成随机数，以后可以通过函数将它转换成不同的数据类型。
泛型:类型的类型，可以在一个函数中利用泛型定义参数，可以使函数对于不同的变量类型进行操作。java中不允许创建泛型类型的数组，因为类型擦除，所以不能同一数组中的元素类型不同
collection是一个接口，它提供了对集合对象进行基本操作的通用接口方法，直接继承接口的有list和set集合
collections是一个工具类，不能被实例化，提供了很多的静态方法。
list是一个接口，而arraylist是list接口的一个实现类，我们在创建对象时可以直接创建实现类的对象，List arr=new ArrayList()，这样在以后修改类的类型时很方便
map与hashmap的关系与list和arraylist的关系相同，map是一个接口不能实例化，hashmap是map的一个实现。hashmap的底层结构是哈希表的具体实现，通过相应的哈希运算就可以很快查询到目标元素在表中的位置，拥有很快的查询速度。
今天有事，内容较少。
