shell文件和可执行文件都是用./执行的，c语言文件需要先用gcc进行编译生成可执行文件。添加自定义路径（可以只在当前shell有效也可永久有效，将路径以追加的方式重定向到文件中.zshrc）到“PATH”环境变量中后，执行脚本或者编译后的可执行文件时不需要./直接文件名即可

搜索文件：
whereis：只能搜索二进制文件，man帮助文件和源代码文件
locate：最好先更新一下数据库，可以自动递归子目录进行查找，快而全
which：只从PATH环境变量指定的路径中去搜索命令
find：可以通过文件类型、文件名进行查找而且可以根据文件的属性（如文件的时间戳，文件的权限等）进行搜索find[path][option][action]

异常分类：
（1）error：系统内部错误，这类错误由系统进行处理，程序本身无需捕获处理
（2）exception：可以处理的异常
（3）runtimeexception：可以捕获也可以不捕获的异常
（4）继承exception的其他类：必须捕获，通常在API文档中会说明这些方法抛出哪些异常
exception下的异常主要分为两大类：运行时异常和受查异常（非运行时异常），受查异常是编译器要求必须处理的异常，必须使用try-catch处理，或者向上抛出，给上层处理

try,catch,finally程序块，try中放可能出现异常的代码，如果执行完try且不发生异常，则执行finally后面的代码；若发生异常，则尝试匹配catch。catch块是用于处理异常的，按顺序从上向下匹配子类在前，父类在后



明天我将去济南进行新兵训练营，接下来可能不能及时更新学习进度，学习进度很慢，我会在以后的时间抓紧赶上
